/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.imgur.com",
      "placehold.co",
      "placeimg.com",
      "via.placeholder.com",
    ],
  },
};

export default nextConfig;
